{
  "framework": null,
  "buildCommand": "pnpm exec prisma generate && pnpm exec tsc -p tsconfig.json",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
